namespace Core.Common
{
    public static class Enums
    {
        public enum ApplicationRole
        {
            Admin = 1,
            Manager = 2,
            User = 3,
            Student = 4,
            SuperAdmin = 5
        }
        public enum ImageFormat
        {
            bmp,
            jpeg,
            gif,
            tiff,
            png,
            pdf,
            unknown
        }

    }
}
